git init. = cria um repositório git na pasta atual
git status = mostra o status do trabalho (de cada arquivo)
git add nome_arq = adiciona o arquivo para ser commitado
git commit -m "mensagem" = faz commit do que estava para ser commitado
git log = mostra todo o histórico do commit